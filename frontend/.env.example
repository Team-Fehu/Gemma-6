# Only needed for production builds where the frontend and backend are on
# different origins (e.g. frontend on Vercel, backend self-hosted behind a
# Cloudflare Tunnel). Leave unset for local dev — Vite's proxy in
# vite.config.ts handles /api locally.
VITE_API_BASE_URL=https://your-tunnel-subdomain.trycloudflare.com
